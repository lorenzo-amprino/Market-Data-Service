spring:
  datasource:
    url: ${MARKET_DATA_DB_URL:jdbc:postgresql://localhost:5432/market_data}
    username: ${MARKET_DATA_DB_USERNAME:market_data}
    password: ${MARKET_DATA_DB_PASSWORD:market_data}
  jpa:
    show-sql: false
